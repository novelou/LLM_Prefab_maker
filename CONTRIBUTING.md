# 貢献ガイド

バグ報告、文書の改善、再現可能な修正を歓迎します。まず [README](README.md) と [仕様書](docs/SPECIFICATION.md) を参照してください。

## 開発の流れ

1. 公開先リポジトリをfork / cloneし、変更用ブランチを作成します。
2. `npm ci`、`npm run dev` で起動します。
3. 問題に必要な範囲で変更し、既存のコードスタイルに合わせます。
4. `npm run build` と `npm test` を実行します。UI / Worker / GLB / 履歴の変更はChromeを用意してE2Eも実行します。
5. 振る舞いの変更を仕様・変更履歴に反映し、PRに問題、変更後の動作、検証結果を記載します。

整形はPrettierを使用します。依存を変更した場合はpackage-lock.jsonも更新し、`npm run licenses` を実行してください。新しい依存は用途、バージョン、ライセンスを説明してください。

## バグ報告

OS、Node.js、Chrome / Edgeのバージョン、手順、期待した結果、実際の結果、エラーコードを記載してください。モデル品質の問題では、指示、seed、配信モデル名、設定、問題が見える画像も有用です。公開できないキー、内部アドレス、入力画像を含めないでください。

サーバーのHTTP障害、コード実行の失敗、見た目の不一致は別の問題として記録してください。GLB保存成功だけで形状が正しいと判断しないでください。

## PRの範囲

自由なthree.jsコード表現、ローカル利用、完全な応答だけの実行、失敗時の現行版保持を維持します。隔離やリソース制限を緩める変更は、理由と実際の保護範囲を説明してください。

通常のテストで実WSの推論を開始しないでください。ベンチマークは明示的に行い、プロンプト・ランタイム・設定・試行数を記録します。元の失敗例を消さず、改善した部分と回帰を両方示します。

## ライセンス

貢献はこのプロジェクトの [MIT License](LICENSE) の下で提供してください。第三者のコード・画像を加える場合は出典と必要なライセンス通知を含めます。モデルの重み、APIキー、非公開ログは追加しません。別途のCLAは設けていません。

脆弱性は [SECURITY.md](SECURITY.md) の経路で報告してください。
