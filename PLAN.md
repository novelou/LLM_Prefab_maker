# Qwen × three.js モデル生成・GLB出力 計画

作成日: 2026-09-07 / 更新日: 2026-09-10 / 状態: 初期実装・30件PoC・公開用文書の整備済み。

この文書は当初の設計計画です。現行の実装・上限は [仕様書](docs/SPECIFICATION.md)、実測と未完了項目は [PoCレポート](POC_REPORT.md) を正とします。段階1の校正GLBとBlender再読込、段階2の30件評価、段階3のGUIを実装しました。段階2の画像修正品質評価は未完了。ストリーミングの代わりに経過時間と処理状態を表示し、OSレベルのメモリ・GPU隔離は実装していません。

## 1. 結論

実現可能。まずは「自然文から静的な3D小物を生成し、回転プレビューで確認・追加指示で修正して、GLBを保存する」ローカルアプリを目指す。

推奨構成は **React + TypeScript + three.js のブラウザGUI、Node.jsのローカルAPI**。画面はプロンプト・API設定・プレビューの3パネルで完結させる。

生成経路は `プロンプト → Qwen API → three.jsコード生成 → 隔離実行・プレビュー → GLB保存` とする。保存したGLBはBlender等で読み込み・編集する。

最大の不確実性は、希望する形状をQwenがどこまで作れるか。ビジュアル理解・コード生成の性能から3D制作への応用は期待できるが、良質な立体形状やトポロジーを保証する根拠にはならない。初期段階で実モデルを評価する。

## 2. 想定するモデルと限界

| 対象 | 初期の見込み・扱い |
| --- | --- |
| 家具、小物、建物、ローポリの乗り物、簡単なメカ | 試作対象。部品の組み合わせ・押し出し・回転体と相性がよいという設計上の見立て |
| 曲面を含む装飾品、デフォルメした生物 | 自由なコード生成による試作対象。品質は実測して判断する |
| 写実的な人物、髪、服、精密な有機形状 | 初期の品質保証対象外。専用3D生成や手修正が必要になりやすい |
| リグ、スキニング、アニメーション、製造用CAD | MVP対象外 |

画面上の陰影・反射・背景による見栄えと、書き出すメッシュの品質を区別する。初期モデルは複数メッシュのグループを許容し、結合済み・水密・四角面主体のメッシュを保証しない。3Dプリント用途は別の要件として扱う。

## 3. Qwen APIの扱い（ユーザー指定を反映）

対象は **Qwen3.8-Flash-Nextそのもの**。ユーザーが共有ワークステーション（WS）上でvLLMを起動し、アプリはそのOpenAI互換APIへ接続する。マネージドサービス版への置き換えは行わない。[対象モデルの公式リポジトリ](https://github.com/QwenLM/Qwen3.8-Flash-Next/)

vLLMはOpenAI互換のHTTPサーバーを提供する。初期は `/v1/chat/completions` を利用し、モデル一覧は `/v1/models` で確認する。[vLLM公式資料](https://docs.vllm.ai/en/latest/serving/online_serving/openai_compatible_server/)

接続構成:

```text
利用PC: ブラウザGUI → ローカルNode.js → 共有WS: vLLM / Qwen3.8-Flash-Next
          ↓
       GLB保存 → 必要に応じてBlenderで編集
```

WSは推論、利用PCは描画・GLB書き出し・ファイル保存を担当する。ブラウザからWSへ直接接続する構成にはしない。vLLMの起動・モデル配置・GPU割当はユーザー側の管理範囲とし、このアプリは起動済みサービスのクライアントとして実装する。

API設定パネル:

- Base URL: 例 `http://<共有WSのホスト名>:8000/v1`。実際のホスト・ポート・プロキシ配下のパスを自由に設定する。
- Model ID: vLLMの公開名に一致させる。`--served-model-name` を設定する場合はその値を使用し、モデル一覧からの選択と自由入力を用意する。
- API Key: 任意。WS側に認証がある場合のみ設定し、マスク表示する。未設定の場合はAuthorizationヘッダーを送らない。
- 「接続テスト」: 到達性・モデル一覧確認と、小さな生成リクエストによる確認。追加の推論が走ることを表示する。
- 詳細設定: 出力トークン上限、タイムアウト、temperature、対応確認済みの場合のみthinking関連設定。
- コード応答・画像入力は、実際のvLLM版、モデル実装、起動設定に合わせて検証する。QwenCloud固有パラメーターを流用しない。


APIキーはNode.js側で保持し、初期はセッション中のみ保存。キー以外の設定をローカル保存する。ブラウザのlocalStorageや生成プロジェクト、ログにはキーを入れない。永続保存は後続でWindows資格情報ストア等を検討する。

共有WSでは他利用者とのGPU競合・待ち時間を想定し、アプリ側は生成の同時実行を1件に制限する。タイムアウトを変更可能にし、自動修復は最大1回、接続失敗時に無制限の再送を行わない。利用したトークン数・応答時間を記録する。

Flash-Nextのロード可否、必要なvLLM版、chat template、量子化、コンテキスト上限、画像入力の可否はWSの実構成で確認する。検証済みの版・起動条件を記録し、未検証の起動コマンドを確定手順として掲載しない。

## 4. GUI方式の比較と推奨

| 方式 | 利点 | 負担 | 判断 |
| --- | --- | --- | --- |
| ローカルWeb GUI + Node.js | three.jsをそのまま利用。API通信とキー管理をサーバー側で扱える | 起動用スクリプトとローカルサーバーが必要 | MVPに採用 |
| Electron | 独立ウィンドウ、ファイル選択、配布をまとめやすい | パッケージ容量、IPCと配布作業が増える | 単体アプリ化の候補 |
| Tauri | デスクトップUIを構成できる | Rust側とプロセス連携の実装が加わる | 初期には採用しない |
| ブラウザのみ | 配置が簡単 | 共有WS側のCORS設定とブラウザでのキー保持が必要 | 可能だが初期はローカルAPIを併用 |

「3つの窓」は、初期は1つの画面内の3パネルと解釈する。独立したOSウィンドウが必要なら、後でElectron化して分離する。

```text
┌──────────────────────┬──────────────────────────────────┐
│ プロンプト            │ プレビュー                        │
│                      │                                  │
│ 指示入力              │ 回転・ズーム・パン                 │
│ [生成] [修正] [停止]  │ 正面 / 側面 / 上面 / 全体表示       │
│ 処理状態・履歴         │ ワイヤーフレーム / グリッド         │
├──────────────────────┤                                  │
│ API設定              │ 寸法・三角形数・メッシュ数          │
│ URL / Key / Model    │ [GLB保存] [プロジェクト保存] │
│ [接続テスト]          │                                  │
│ 詳細設定     │                                  │
└──────────────────────┴──────────────────────────────────┘
```

左側を約360px、右側を残り幅とするCSS Gridが出発点。狭い画面では縦積みにする。Reactはフォーム・ジョブ状態を管理し、three.jsは専用コンポーネントで直接扱う。初期は大規模なUIライブラリや3Dエディタ機能を導入しない。

プレビューにはOrbitControls、標準照明、背景、床グリッド、自動フレーミングを用意する。床・照明・カメラ・補助線はモデル本体と分離し、GLBに含めない。プレビュー操作はモデルの座標を変更しない。

## 5. モデルを生成する方式（2026-09-10改訂）

### 主方式: three.jsコードを直接生成する

Qwen3.8-Flash-Nextのコードによる造形・視覚表現を活かすため、JavaScriptモジュールを直接生成して実行する。あらかじめ列挙した形状をJSONで選ばせる設計は採用しない。アプリは実行・プレビュー・修正・出力の環境を提供し、形状の作り方はモデルに任せる。

生成コードは、ループ、関数、数式、独自アルゴリズム、BufferGeometryによる任意の頂点・面、手続き的なテクスチャ、マテリアル、three.jsの機能を利用できる。造形用の固定DSLや基本形状のホワイトリストを設けない。three.jsのバージョンと提供するアドオンを明示し、追加依存が必要な場合は依存として扱う。

ホストとの接点だけを小さく定める。例えば `createModel({ THREE, addons, seed })` が `modelRoot` と任意のプレビュー設定・後処理関数を返す。これはインターフェース案であり、形状を制限するスキーマではない。単位、上方向、書き出すルートを揃え、内部の構築方法は自由とする。

APIからはソースコードをテキストで受け取る。実行エラーや書き出しエラーを元コードと一緒にQwenへ返して修正する。途中で切れたソースは実行しない。最大1回の自動修復を初期値とし、以後は追加指示で続行する。

### JSONで同じ自由度を持てるか

JSONはデータの記法であり、それ自体が表現力を決めるわけではない。任意の頂点・面・属性を格納すれば、静的なメッシュは広く表現できる。しかし、前案のプリミティブ・押し出し等の固定スキーマは、その機能の範囲に造形を制限する。

任意のメッシュを生の数値配列で出力させる方式も、短い関数や数式で大量の頂点を作れるコードに比べて冗長になりやすい。任意の計算までJSONで記述できるようにすれば独自のプログラミング言語と実行器に近づき、three.jsコードを直接生成する利点が薄れる。JSON内にコード文字列を入れるなら、実体はコード生成方式である。

したがってJSONは保存用メタデータや実行結果の受け渡しに必要なら使うが、Qwenの造形言語としては使わない。

### 実行環境と出力互換性

造形の自由度とホストへのアクセス権を分ける。生成コードはAPIキーやファイル操作権限を持たない独立した実行コンテキストで動かす。隔離プレビューとメッセージ連携、CSP、停止・再作成の方式をPoCで検証する。iframeを置くだけで無限ループやGPU負荷まで隔離できるとは仮定しない。停止が効く構成を採用し、必要なら別プロセスへ分離する。

実行時間・メモリ・生成規模には変更可能な運用上の予算を設ける。基本形状の種類や作り方には制約を設けない。単純なevalやNode.jsのvmを安全な隔離と見なさない。

プレビューでは自由な表現を許容するが、GLBが任意のプログラムやシェーダーを保存できるとは扱わない。書き出し時に生成済みシーンを検査し、必要な変換と未対応表現を報告する。形状をCPU側メッシュへ確定する、質感をテクスチャへベイクする等の対処は対象ごとに検討する。任意シェーダーの自動ベイクはMVPの保証範囲外。未対応の表現を黙って削除して成功と表示しない。

### 修正と視覚フィードバック

追加指示では現在のソースと新しい指示をAPIへ送り、改訂ソースを受け取る。実行成功後にプレビューを置換し、直前の版へ戻せるようにする。ソース・依存バージョン・seed・必要なアセットを保存し、再現できるようにする。

画像入力がWS側で利用できる場合、正面・側面・斜めのプレビューを元ソースとともにQwenへ送り、形状を見ながら修正する機能を初期PoCで試す。生成 → 描画 → 視覚確認 → コード修正を中心の反復手順とする。ユーザー操作ごとの修正を基本とし、無制限の自動改善ループは組まない。

## 6. GLB出力

three.jsはglTFを推奨しており、GLTFExporterがGLB出力を提供している。GLBを最終出力形式に採用する。[three.js FAQ](https://threejs.org/manual/en/faq.html)・[GLTFExporter](https://threejs.org/docs/pages/GLTFExporter.html)

保存したGLBはBlenderで読み込み、編集できる。Blenderはユーザーが必要に応じて使う外部ツールであり、アプリの実行依存にはしない。[BlenderのインポートAPI](https://docs.blender.org/api/main/bpy.ops.import_scene.html)

処理:

1. 現在表示しているモデルの版を固定する。
2. モデル本体のシーンを検査し、未対応のマテリアルや属性があれば表示する。
3. 隔離実行側でモデルGroupのみをGLTFExporterのbinary出力でGLBにする。
4. 出力サイズ等を確認し、ArrayBufferをホストへ渡してブラウザから保存する。
5. 完了・失敗・キャンセル時に一時バッファとダウンロード用Object URLを解放する。

Node.jsへのファイル転送、外部変換プロセス、Blenderのパス設定は不要。Node.jsはAPI接続と設定・ジョブ管理を担当する。

最初の試験は基本色の静的メッシュで行い、その後、標準PBRマテリアルと画像テクスチャを含むGLBを検証する。必要な画像はGLB内に含め、外部ファイルなしで読み込めることを確認する。生成コードの表現範囲は試験条件に限定しない。

GLBにも任意のJavaScript、ポストエフェクト、独自シェーダーをそのまま保存することはできない。プレビューの自由度は維持し、書き出し時に保持できる内容と未対応の内容を明示する。保存後の再読込ではメッシュ・基本的な質感・テクスチャを確認し、レンダラー間での完全な画素一致は要求しない。

モデルはメートル単位・Y-upを基準とし、非対称な校正モデルで寸法・方向を確認する。複製配置や負スケールは生成時には禁止せず、書き出しと再読込で法線・面の向きまで検証する。必要な正規化はエクスポート用の複製へ適用し、表示中のモデルを変えない。

## 7. ローカル構成と状態管理

```text
model_maker/
  PLAN.md
  package.json
  src/                 React GUI・three.js viewer
  server/              設定・Qwen接続・ジョブ管理
  runtime/             生成コードの隔離実行・プレビュー連携
  shared/              実行契約・出力検査・共通型
  scripts/             開発・検証補助
  tests/fixtures/      校正モデル・生成コードの検証用例
  README.md            起動・API設定・GLB保存
```

Node.jsは127.0.0.1だけで待ち受け、ブラウザとは同一オリジンで接続する。ローカルAPIにはOrigin確認とセッショントークンを設ける。生成物は任意URLを読み込まず、APIキーを含むログを残さない。

ジョブ状態は待機・API応答中・検証中・プレビュー準備・書き出し中・完了・失敗・キャンセル。初期は生成1件、書き出し1件まで。多重クリックと古い応答によるモデル上書きを防ぐ。ストリーミングは進捗表示に使い、未完成ソースを実行しない。

通信失敗、認証エラー、レート制限、モデル未対応、出力上限、構文エラー、実行エラー、書き出し失敗を区別して同じ3パネル内へ表示する。停止操作で通信やローカル処理を中止するが、共有WSで実行開始済みの推論が直ちに停止するとは限らない。失敗時も最後に成功したモデルを保持する。

プロジェクトはソース・プロンプト・依存バージョン・seed・アセット・キーを除くモデル識別情報をまとめたアーカイブとして保存・復元する。メタデータにはJSONを使ってよい。GLBだけでは元の生成コードを復元できないため、プロジェクト保存を別途用意する。

## 8. 実装段階と完了条件

### 段階1: GLB保存・再読込のPoC

- 固定の非対称モデルをthree.jsで表示し、GLB保存まで通す。
- 1mの校正形状、色分け、階層、押し出し、回転体、複製を含める。
- 保存したGLBをthree.jsの新規シーンとBlenderへ読み込み、寸法・方向・色・階層・法線・モデル欠落を確認する。
- 暫定数値基準: 校正寸法の誤差1%以内、鏡像化や余分な床・カメラなし。
- 完了条件: 保存だけでなくGLB再読込が成功し、上記条件を満たす。

### 段階2: Qwen生成のPoC

- API設定、コード受信、隔離実行環境、シーンの出力検査を実装する。画像入力対応時はマルチビューを使う修正も検証する。
- 椅子、机、家、塔、ロボット小物、車、ランプ、花瓶、木、SFコンテナの10プロンプトを各3回評価する。
- 初回有効率、修復後の有効率、形状要件の充足、生成時間、使用トークン、GLB出力成功率を記録する。
- 暫定継続基準: 最大1回の修復込みで30件中27件以上が表示・GLB出力可能。10題中8題以上で、3回中2回以上は事前に定めた主要な形状・部品・色の条件を満たす。
- 不十分なら、プロンプト・提供ドキュメント・実行エラーのフィードバック・視覚修正を改善する。固定形状スキーマへ戻すことを既定の対策にはしない。性能値は目標であり、現時点の実績ではない。

### 段階3: 3パネルMVP

- プロンプト入力、API設定と接続テスト、プレビュー操作。
- 追加指示、直前版への復帰、停止・エラー表示。
- GLB・プロジェクト保存とプロジェクト読み込み。
- ローカル起動スクリプト、README。Blender未インストールでも全機能を利用できる。
- 完了条件: GUIだけでAPI設定 → 生成 → 修正 → GLB保存まで操作できる。

### 段階4: 品質を上げる拡張

- 視覚修正の操作性向上、参照画像入力。
- 出力時のテクスチャ保持・ベイク、追加ライブラリの導入支援。自由形状は初期からコードで表現可能とする。
- GLBの出力最適化、Electron化、APIキーの永続保存。

段階1〜3が初期実装範囲。段階4はMVP評価後に選択する。

## 9. 必要な検証

- 実行環境: 構文エラー・例外・無限ループ・過大な形状生成から停止・復帰でき、生成コードがキーやローカルAPIへアクセスできない。
- API: 401・429・タイムアウト・途中終了・不完全なコード応答で画面が復帰する。
- 履歴: 失敗応答が成功モデルを消さず、修正と戻す操作で同じ版を復元できる。
- 出力: 画面のモデルと書き出す版が一致し、GLB再読込で校正形状が維持される。
- リソース: モデル置換時にGeometry・Materialを解放し、書き出し後に一時バッファ・Object URLを解放する。
- 利用先がUnity・Unreal等と決まった時点で、そのアプリでの読み込み確認を追加する。Blender再読込だけで全ツール互換とは判定しない。

## 10. 現時点の仮定と未確定事項

- Windowsで個人利用するローカルアプリ。初期は公開ホスティングやユーザー認証を必要としない。
- BlenderはGLBの読み込み確認・編集用の任意ツール。アプリによる自動起動やインストール要件は設けない。
- 対象は静的な小物・建物・ローポリモデル。希望する作例でPoCの評価題材を差し替えられる。
- 推論先は共有WS上のvLLM / Qwen3.8-Flash-Nextで確定。URL・公開モデル名・認証の有無・vLLM版は実装時の接続テストで確認する。
- GLBをBlenderで扱う想定。ほかの利用先が必要になった場合は追加で互換性を検証する。
- 実装工数はAPI応答品質とGLBの出力互換性に依存するため、段階1・2の結果で見積もる。

追加ベンチマークと最適化は終了し、公開版は評価済みv2プロンプトを採用します。段階4は将来の候補で、現在の提供機能には含めません。参照画像入力は先行実装しています。導入・公開方法は [README](README.md) と [開発ガイド](docs/DEVELOPMENT.md) を参照してください。



