# セキュリティとデータの扱い

## 想定する利用

v0.1.0は単一ユーザーのローカル利用向けです。Node.jsは127.0.0.1だけで待ち受けます。インターネット公開、リバースプロキシ経由の共有、複数ユーザー認証は設計対象外です。

LLMが生成するJavaScriptと `.qmodel` 内のソースは実行可能なコードです。プロジェクトを開くと、そのソースを隔離Workerで再実行します。信頼できない配布物を単なる画像ファイルとして扱わないでください。

## 実装している対策

- ローカルAPIのHost / Origin / Fetch Metadata確認とセッショントークン。
- APIキーをサーバーメモリだけに保持。設定保存後は入力欄を空にし、API応答・プロジェクト・設定ファイルへキーを返さない。
- 不透明オリジンiframeが所有するDedicated Workerでのコード実行。
- CSPによるWorkerのHTTP通信遮断。テクスチャ再読込用のblob / dataだけを許可。
- オリジン分離によるストレージアクセスの拒否、生成コードに渡さないMessagePort。
- 親側が管理する期限・停止、応答サイズ制限、形状・テクスチャ・アーカイブの検査。
- 上流エラー本文をそのまま表示せず、一般化したメッセージへ変換。

## 境界と残る制限

WorkerとiframeはOSレベルのサンドボックスではありません。GPUプロセスは共有され、極端なメモリ割当を構築前に完全防止できません。形状のメモリ検査は生成後です。CPUループの停止と、GPU / メモリ枯渇への完全な耐性を同一視しないでください。

ローカルの別プロセスからのアクセスを本人認証する設計ではありません。信頼できないローカルプロセスやブラウザ拡張、ブラウザ自体の脆弱性は保護範囲外です。保存済みプロジェクトのコードは、プロンプト文に書かれた禁止事項だけでは安全性を証明できません。

## データの送信・公開

指示、現在ソース、添付画像、修復用エラーは設定した推論先に送られます。HTTPを選んだ場合にアプリが独自に暗号化することはありません。接続先のログ・保存方針は接続先の管理下です。

`.local/` には接続先、生成ソース、画像、評価ログが入り得るため公開対象外です。`.qmodel` も指示や画像を含みます。IssueやPRへ添付する前に内容を確認してください。リリース用スクリプトは明示したソース・文書だけを梱包しますが、任意の秘密情報を完全に検出する保証はありません。

## 脆弱性の報告

公開先リポジトリでSecurity Advisoryの非公開報告が有効なら、その経路を使用してください。無効の場合は、まず機密情報や攻撃コードを含めないIssueで非公開の連絡経路を依頼してください。本ソースでは未確定のメールアドレスや組織窓口を記載していません。

バージョン、OS / ブラウザ、影響、最小限の再現手順を提供してください。実際のキー・個人情報は添付しないでください。対象は現在の0.1系で、修正期限やSLAの保証はありません。
