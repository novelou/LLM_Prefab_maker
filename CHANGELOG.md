# Changelog

## 1.0.1 — 2026-09-26

### 追加修正の部分編集

- 追加指示による修正で、既存の数行だけを変える場合は行指定の部分編集を使えるようにしました。モデルの修正応答を短くできます。新しい部品の追加や複数箇所にまたがる変更では、従来どおりソース全文を返すよう指示します。
- 部分編集を選んだ場合は、指定行の番号と元テキストを照合してから適用します。形式や行照合に失敗した場合は元ソースを保持し、失敗理由を伝えて最大3回試します。ソース全文の応答も受け付けます。
- 追加修正と自動修復の応答形式を確認するサーバー試験・ブラウザ試験を追加しました。実モデルを使う追加試験は推論先を指定した場合に実行します。

既存の設定と `.qmodel` の形式は変更していません。修正結果の品質は接続先モデルに依存します。

## 1.0.0 — 2026-09-25

### 生成のストリーミング表示と診断

- AIが生成中の内容を、折りたたみ式の「生成の詳細」に逐次表示します。推論先が推論テキストを提供する場合は、その内容も確認できます。
- 生成途中で意図から外れた推論や重大な判断ミスを見つけ、次回のプロンプトを調整しやすくなります。
- 失敗時にも受信済みの出力、失敗した段階、終了理由、使用トークンを確認できます。生成コードは応答が正常に完了して検証が通るまで実行しません。
- Reasoning effortを「API既定」「none」「low」「medium」「xhigh」から選択できます。選択値は対応する推論サーバーへの生成要求に適用します。

### 行指定の自動修復

- 元ソースに行番号を付けてモデルに渡し、返された部分編集の行番号と元行を厳密に照合してから適用します。広範囲の修正はソース全文でも受け付けます。
- 修復編集の形式や行照合が不正な場合は、元ソースを保持したまま編集応答を最大3回試します。編集が適用された後の実行・検査エラーは追加修復しません。

### 互換性と制限

- 既存の設定と `.qmodel` の形式は変更していません。Reasoning effortの初期値は「API既定」です。
- 推論テキストの表示とReasoning effortの受理は接続先モデル・サーバーに依存します。3D形状・寸法の正確性、複数方向画像による修正品質、GPUとメモリのOSレベル隔離については、従来の制限が残ります。

## 0.1.0 — 2026-09-10

初回のソース公開準備版。ローカル利用向けの実験的MVPです。

- React / three.jsの3パネルUIとNode.jsローカルAPI。
- OpenAI互換サーバーによるコード生成、追加指示、参照画像、3方向画像の添付。
- Worker隔離、期限と停止、候補モデルの検査、最大1回の修復。
- GLB保存・再読込検査、プロジェクト保存・復元、履歴20版。
- 校正モデル、API試験、ブラウザE2E、30件の初期PoCと16件のv2評価。
- 公開版は評価済みv2プロンプトで固定。v3の探索結果は採用せず記録。
- 初期接続先を汎用localhost、Model IDを空欄、reasoningをAPI既定へ変更。既存の `.local/settings.json` は保持。
- MITライセンス、依存通知、仕様・運用・貢献ガイド、ソース配布用スクリプト。
- 公開前の依存監査に対応し、fflate 0.8.3、Vite 7.3.6、Playwright 1.55.1へ更新。既存の保存・復元・ブラウザ試験を再確認。

既知の制限: 寸法・造形の不一致、画像修正品質評価の未完了、GPU / メモリのOSレベル隔離なし。詳しくは [仕様書](docs/SPECIFICATION.md) と [検証レポート](POC_REPORT.md) を参照してください。
